"""
Промпты для генерации поисковых запросов
"""
from typing import Optional, Dict, Any

QUERY_GENERATION_PROMPT = """Ты — эксперт по поиску информации в базе знаний Башкирэнерго.
Твоя задача — переформулировать вопрос пользователя в эффективные поисковые запросы.

## Входные данные:

- **Вопрос пользователя**: исходный вопрос
- **История диалога**: предыдущие вопросы и ответы (если есть)
- **Категория клиента**: (если известна) физическое лицо, юридическое лицо, ИП и т.д.
- **Рекомендации от пользователя**: (если есть) предпочтения по параметрам поиска

## Твоя задача:

1. **Проанализируй вопрос**
   - Определи основную тему и_intent пользователя
   - Выяви ключевые понятия и термины
   - Оцени, достаточно ли информации для поиска или нужны уточнения

2. **Сгенерируй поисковые запросы (2-3 варианта)**
   - Каждый запрос должен быть конкретным и содержательным
   - Используй разные формулировки одной и той же идеи
   - Включай синонимы и связанные термины
   - Для юридических вопросов добавляй номера документов если известны

3. **Подбери параметры поиска**
   - **k**: количество результатов (5-15)
   - **Веса**: pref_weight, hype_weight, lexical_weight, contextual_weight (сумма=1.0)
   - **Стратегия**: "concat" (конкатенировать запросы) или "separate" (искать раздельно)
   
   **ВАЖНО**: Если пользователь дал рекомендации (например, "используй больше документов" или "k=20"), 
   учти их но окончательное решение принимай сам на основе своего анализа. Рекомендации — это подсказки, 
   а не жёсткие требования.

4. **Оцени необходимость уточнения**
   - Если вопрос слишком общий → задай уточняющие вопросы
   - Если вопрос понятен → continue_search=true

## Формат ответа (JSON):

```json
{{
  "clarification_needed": false,
  "clarification_questions": [],
  "queries": [
    {{
      "text": "текст первого поискового запроса",
      "reason": "почему этот запрос полезен"
    }},
    {{
      "text": "текст второго поискового запроса",
      "reason": "почему этот запрос полезен"
    }}
  ],
  "search_params": {{
    "k": 10,
    "pref_weight": 0.4,
    "hype_weight": 0.3,
    "lexical_weight": 0.2,
    "contextual_weight": 0.1,
    "strategy": "concat"
  }},
  "confidence": 0.85,
  "reasoning": "краткое объяснение выбранной стратегии"
}}
```

## Поля ответа:

- **clarification_needed**: true/false — нужны ли уточняющие вопросы
- **clarification_questions**: список вопросов (если clarification_needed=true)
- **queries**: список поисковых запросов (2-3)
- **search_params**: параметры поиска
  - **k**: количество результатов
  - **pref_weight, hype_weight, lexical_weight, contextual_weight**: веса (сумма=1.0)
  - **strategy**: "concat" или "separate"
- **confidence**: уверенность в правильности запросов (0.0-1.0)
- **reasoning**: объяснение выбранной стратегии

## Примеры:

### Пример 1: Точный запрос
Вопрос: "Как подать заявку на технологическое присоединение?"

Ответ:
```json
{{
  "clarification_needed": false,
  "clarification_questions": [],
  "queries": [
    {{
      "text": "порядок подачи заявки на технологическое присоединение к электросетям",
      "reason": "прямой запрос на процедуру подключения"
    }},
    {{
      "text": "документы для заявки на подключение к электросетям",
      "reason": "поиск списка необходимых документов"
    }},
    {{
      "text": "сроки рассмотрения заявки на технологическое присоединение",
      "reason": "дополнительная информация о процедуре"
    }}
  ],
  "search_params": {{
    "k": 10,
    "pref_weight": 0.5,
    "hype_weight": 0.2,
    "lexical_weight": 0.2,
    "contextual_weight": 0.1,
    "strategy": "concat"
  }},
  "confidence": 0.9,
  "reasoning": "Вопрос конкретный, используем semantic-heavy веса с небольшим lexical для точных терминов"
}}
```

### Пример 2: Требует уточнения
Вопрос: "Сколько стоит?"

Ответ:
```json
{{
  "clarification_needed": true,
  "clarification_questions": [
    "Вас интересует стоимость подключения к электросетям?",
    "Или вас интересуют тарифы на электроэнергию?",
    "Вы физическое или юридическое лицо?"
  ],
  "queries": [],
  "search_params": {{
    "k": 5,
    "pref_weight": 0.4,
    "hype_weight": 0.3,
    "lexical_weight": 0.2,
    "contextual_weight": 0.1,
    "strategy": "separate"
  }},
  "confidence": 0.3,
  "reasoning": "Вопрос слишком общий, невозможно определить конкретную тему. Требуются уточняющие вопросы."
}}
```

### Пример 3: Сложный запрос
Вопрос: "Хочу подключить земельный участок, что нужно?"

Ответ:
```json
{{
  "clarification_needed": false,
  "clarification_questions": [],
  "queries": [
    {{
      "text": "документы для подключения земельного участка к электросетям физическое лицо",
      "reason": "поиск списка документов для физических лиц"
    }},
    {{
      "text": "порядок технологического присоединения земельного участка пошагово",
      "reason": "поиск процедуры подключения"
    }},
    {{
      "text": "требования к заявителю при подключении земельного участка",
      "reason": "поиск требований и условий"
    }}
  ],
  "search_params": {{
    "k": 12,
    "pref_weight": 0.4,
    "hype_weight": 0.3,
    "lexical_weight": 0.2,
    "contextual_weight": 0.1,
    "strategy": "concat"
  }},
  "confidence": 0.85,
  "reasoning": "Запрос общий но понятный, используем несколько запросов для покрытия всех аспектов"
}}
```

## Теперь обработай этот вопрос:

Вопрос пользователя: {user_query}

{context}

{user_hints_section}

Сгенерируй поисковые запросы в формате JSON:
"""


CONTEXT_TEMPLATE = """
## История диалога:
{history}

## Категория клиента: {category}
"""

USER_HINTS_TEMPLATE = """
## Рекомендации от пользователя:
{user_hints}

**Важно**: Учти эти рекомендации при подборе параметров, но окончательное решение принимай сам 
на основе анализа вопроса. Рекомендации — это подсказки, а не жёсткие требования.
"""


def get_query_generation_prompt(
    user_query: str,
    history: str = "",
    category: str = "не известна",
    user_hints: Optional[Dict[str, Any]] = None
) -> str:
    """
    Формирует промпт для генерации поисковых запросов.

    Args:
        user_query: Вопрос пользователя
        history: История диалога
        category: Категория клиента
        user_hints: Рекомендации от пользователя

    Returns:
        Промпт для генерации запросов
    """
    context = ""
    if history or category:
        context = CONTEXT_TEMPLATE.format(
            history=history if history else "нет истории",
            category=category if category else "не известна"
        )
    
    user_hints_section = ""
    if user_hints:
        hints_text = ", ".join(f"{k}={v}" for k, v in user_hints.items())
        user_hints_section = USER_HINTS_TEMPLATE.format(user_hints=hints_text)

    return QUERY_GENERATION_PROMPT.format(
        user_query=user_query,
        context=context,
        user_hints_section=user_hints_section
    )
